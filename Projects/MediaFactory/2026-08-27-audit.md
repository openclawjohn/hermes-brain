# Media Factory — Audit (2026-08-27)

**Server:** 192.168.7.100 · `/srv/apps/media-factory/`
**Task:** Audit what exists, what's wrong, and how to fix it, against the V1 spec brief.

## What is there
- Working Django 5.x app, Python 3.12, **SQLite** (`db.sqlite3`), venv at `venv/`.
- Apps: dashboard, campaigns, programmes, content, library, assets, brand, editorial_calendar, publishing, configuration.
- Git: branches main/develop/feature/campaign-planner/feature/campaign-planner-v041 — **all at 33aa91c**.
- **Committed HEAD (33aa91c "Release 0.4.2") is a working app** — verified: `check` passes, `/`, `/programmes/`, `/campaigns/` all HTTP 200.
- Committed code is substantial: programmes/views.py=1079, content/views.py=906, programmes/models.py=523.

## What is wrong
- **Working tree is broken**: `manage.py check` → `AttributeError: campaigns.views has no attribute 'campaign_setup'`. App cannot start.
- **Root cause**: uncommitted half-finished rewrite gutted the working baseline:
  - programmes/views.py 1079→84, content/views.py 906→49, programmes/models.py 523→93, programmes/forms.py 427→50, programmes/urls.py 208→16.
  - Deleted `aurora/` project package, created untracked `media_factory/` (rename never committed).
- **Missing templates**: `programmes/event_form.html`, `configuration/library_form.html` referenced but absent → would 500.
- **Stray junk**: `cd`, `nano`, `project-files.txt` in repo.
- **Architecture violations vs brief**: no services.py/selectors.py/tasks.py in any app; no PROJECT_STATE/DESIGN_SYSTEM/RULES.md; no Obsidian project doc; uncommitted work on main.

## Mistakes in the brief (spec vs reality)
1. PostgreSQL mandated (B.1/G.2) but code uses SQLite.
2. `apps/` subdir mandated (A.3) but apps at root.
3. 39 models / 16 apps (ai, automation, analytics, notifications, integrations, system, users) — none exist; ~10 apps, far fewer models.
4. UUID PKs mandated (B.1) but code uses integer auto-increment.
5. HTMX+Alpine+Inter+Lucide+8pt grid+13-item sidebar design system (C.3/D) — code has 3-link top nav, Arial inline CSS.
6. "Date Reminders" replaces "Milestones" (8.10) but code still has EventMilestone models.

## How to fix (recommended)
1. Salvage genuinely useful new pieces (CampaignType/CampaignTemplate/CampaignContentItem models + configuration app) onto a feature branch.
2. Restore proven-working baseline (reset to 33aa91c) so app runs again.
3. Rebuild toward spec incrementally per brief roadmap (0.5.0→1.0.0), committing working slices on feature branches, never on main.
4. Add PROJECT_STATE.md, DESIGN_SYSTEM.md, RULES.md + Obsidian project note.
5. Reconcile brief mistakes up front (SQLite-vs-Postgres, integer-vs-UUID, Milestones/Date-Reminders naming).

## Status
- Audit delivered to user. **No changes made** — awaiting user decision on salvage-vs-restore-vs-fix-in-place (clarify timed out).
